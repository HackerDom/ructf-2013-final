{sub_dirs, [
    "src"
]}.

{deps_dir, "deps"}.

{deps, [
    {yaws, "1.95", {git, "git://github.com/klacke/yaws.git", {tag, "yaws-1.95"}}}
]}.

{lib_dirs, ["src", "deps"]}.

{clean_files, ["yaws", "ebin"]}.
